# Capacitor plugins (e.g. @codetrix-studio/capacitor-google-auth) declare
# peerDependencies against older @capacitor/core majors but are fully
# compatible at runtime.  legacy-peer-deps restores npm v6 resolution
# behaviour so CI/Vercel installs succeed without --force.
legacy-peer-deps=true
