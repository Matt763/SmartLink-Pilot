<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for SmartLink Pilot
  ====================================================
  • Enforces TLS 1.2+ for all connections to production domains
  • Pins to Let's Encrypt / ISRG root (Vercel uses Let's Encrypt)
  • No cleartext (HTTP) traffic allowed
-->
<network-security-config>

    <!-- Production domains — strict TLS, no cleartext -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">smartlinkpilot.com</domain>
        <domain includeSubdomains="true">vercel.app</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Firebase / FCM endpoints -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">fcm.googleapis.com</domain>
        <domain includeSubdomains="true">firebase.googleapis.com</domain>
        <domain includeSubdomains="true">firebaseinstallations.googleapis.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Default: trust system CA store, block all cleartext -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

</network-security-config>
